<?php
// Example: POST /session/{session_id}/semester
// Payload: { "name": "First Semester", "start_date": "2024-09-01", "end_date": "2025-01-15" }

use Cfms\Controllers\SessionController;
/*
return function ($app) {
    $controller = $app->getContainer()->get(SessionController::class);
    $app->post('/session/{session_id}/semester', [$controller, 'createSemester']);

};*/

